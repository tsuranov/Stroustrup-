for (i = 0; i < max_length; i++)
	if (input_line[i] == '?') quest_count++;

i = 0;
while (i < max_length)
{
	if (input_line[i] == '?') quest_count++;
	i++;
}

p = input_line;
while (i < max_length)
{
	if ( *p == '?') quest_count++;
	p++;
	i++;
}
